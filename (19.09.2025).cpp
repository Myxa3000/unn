#include <iostream>

int main()
{
	int x1, a, b, c, d, h;
	double y1;
	std::cout << "enter a b c d h\n";
	std::cin >> a >> b >> c >> d >> h;
	if (a == 0 or b == 0)
		std::cout << "a or b is 0";
		exit(0);
	for (int k = 0; k < 11; k++)
	{
		x1 = d + h * k;
	}
	y1 = log(5 + 3 * x1);
	if ()
}
